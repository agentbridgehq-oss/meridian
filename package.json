{
  "name": "meridian",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Meridian Agency — independent AI agency (Voice, Sales, Booking). Not ClaudeCraft.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "openclaw": "node openclaw/daily.mjs",
    "openclaw:deploy": "node openclaw/deploy-agent.mjs",
    "deploy:agent": "node scripts/deploy-agent.mjs",
    "deploy:example": "node scripts/deploy-agent.mjs --config deploy/examples/hvac-full.json",
    "onboard": "node scripts/onboard.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "stripe": "^16.12.0"
  }
}
